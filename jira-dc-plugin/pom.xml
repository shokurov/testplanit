<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.testplanit</groupId>
  <artifactId>testplanit-jira-dc</artifactId>
  <version>0.1.7-SNAPSHOT</version>
  <packaging>atlassian-plugin</packaging>

  <name>TestPlanIt for Jira Data Center</name>
  <description>Shows TestPlanIt test cases, runs, and sessions on Jira issues.</description>

  <properties>
    <jira.version>10.3.13</jira.version>
    <platform.version>7.0.10</platform.version>
    <amps.version>9.12.5</amps.version>
    <!--
      5.1.0, NOT 5.0.2 (platform-deps-7.0.10.pom's declared compile-time
      value). Confirmed on jira.rapidsoft.ru (10.3.13) via Manage apps >
      System apps > "Atlassian Spring Scanner Runtime" > Version: the actual
      LIVE runtime processor on this instance is 5.1.0. A 5.0.2-generated
      component index was never being read correctly by it: EVERY
      constructor-injected type failed (host components AND our own @Named
      beans alike — only zero-arg PingResource worked), because nothing was
      ever wired into the bean registry at all, not because of anything
      wrong with our @Named/@ComponentImport usage.
    -->
    <spring.scanner.version>5.1.0</spring.scanner.version>
    <atlassian.plugin.key>io.testplanit.testplanit-jira-dc</atlassian.plugin.key>
    <!--
      Build and runtime are both Java 17 (Jira 10.3's runtime). Do NOT bump
      spring.scanner.version to a newer major (e.g. 6.x): its maven-plugin
      requires JDK 21 to run AND emits a component index older runtimes
      can't read, which makes the plugin install but fail to enable (0 of N
      modules). Verify any future version change against Manage apps >
      System apps > "Atlassian Spring Scanner Runtime" on the target
      instance, not against platform-deps poms alone — they can disagree
      with what is actually deployed.
    -->
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.atlassian.platform.dependencies</groupId>
        <artifactId>platform-public-api</artifactId>
        <version>${platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.atlassian.jira</groupId>
      <artifactId>jira-api</artifactId>
      <version>${jira.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.atlassian.plugins.rest</groupId>
      <artifactId>atlassian-rest-v2-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.ws.rs</groupId>
      <artifactId>jakarta.ws.rs-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.servlet</groupId>
      <artifactId>jakarta.servlet-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.inject</groupId>
      <artifactId>jakarta.inject-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.atlassian.sal</groupId>
      <artifactId>sal-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.atlassian.templaterenderer</groupId>
      <artifactId>atlassian-template-renderer-api</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.atlassian.plugin</groupId>
      <artifactId>atlassian-spring-scanner-annotation</artifactId>
      <version>${spring.scanner.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- Tests -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.11.4</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>5.15.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <version>5.15.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>3.27.3</version>
      <scope>test</scope>
    </dependency>
    <!--
      javax.ws.rs.core.Response.ok()/.status() route through
      javax.ws.rs.ext.RuntimeDelegate, whose implementation is normally
      supplied by Jira's OSGi container at runtime. Resource unit tests build
      real Response objects outside that container, so a RuntimeDelegate
      provider must be on the test classpath. jersey-common is the smallest
      artifact that supplies one for the javax.ws.rs 2.1 API this platform
      version uses (see the jakarta.ws.rs-api note on PingResource).
    -->
    <dependency>
      <groupId>org.glassfish.jersey.core</groupId>
      <artifactId>jersey-common</artifactId>
      <version>2.41</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>com.atlassian.maven.plugins</groupId>
        <artifactId>jira-maven-plugin</artifactId>
        <version>${amps.version}</version>
        <extensions>true</extensions>
        <configuration>
          <productVersion>${jira.version}</productVersion>
          <productDataVersion>${jira.version}</productDataVersion>
          <enableQuickReload>true</enableQuickReload>
          <instructions>
            <Atlassian-Plugin-Key>${atlassian.plugin.key}</Atlassian-Plugin-Key>
            <Export-Package/>
            <!--
              com.atlassian.jira.plugin.webfragment.conditions is listed explicitly:
              bnd's `*` wildcard only imports packages actually referenced by our
              COMPILED .class files. atlassian-plugin.xml's <condition class="..."/>
              is a plain string bnd never sees, so without this line the package is
              missing from Import-Package and the plugin fails to enable at runtime
              with ClassNotFoundException / PluginParseException on the web-panel
              module (confirmed on Jira 10.3.13 — the whole plugin shows "0 of N
              modules enabled" because one bad module descriptor blocks the rest).
            -->
            <Import-Package>
              com.atlassian.jira.plugin.webfragment.conditions,
              org.springframework.osgi.*;resolution:="optional",
              org.eclipse.gemini.blueprint.*;resolution:="optional",
              *
            </Import-Package>
            <Spring-Context>*</Spring-Context>
          </instructions>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.atlassian.plugin</groupId>
        <artifactId>atlassian-spring-scanner-maven-plugin</artifactId>
        <version>${spring.scanner.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>atlassian-spring-scanner</goal>
            </goals>
            <phase>process-classes</phase>
          </execution>
        </executions>
      </plugin>
      <!-- Expose the resolved mockito-core jar path as ${org.mockito:mockito-core:jar}
           so surefire can load Mockito as a proper -javaagent (below). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.8.1</version>
        <executions>
          <execution>
            <goals>
              <goal>properties</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
        <configuration>
          <!-- Load Mockito 5 as an explicit java agent (its recommended setup on
               JDK 21): without this, Mockito's inline mock-maker self-attaches and
               prints a deprecation notice. -Xshare:off silences the CDS "Sharing
               is only supported..." warning that appears once an agent is attached.
               Keeps test output pristine across all Mockito-based test tasks. -->
          <argLine>-javaagent:${org.mockito:mockito-core:jar} -Xshare:off</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <repositories>
    <repository>
      <id>atlassian-public</id>
      <url>https://packages.atlassian.com/mvn/maven-external/</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
    <!--
      Jira 10.3's BOM (com.atlassian.jira:jira-bom) pins commons-httpclient to
      the Jenkins-forked build `3.1-jenkins-3`, which is published ONLY in the
      Jenkins CI public repository — it is not mirrored in any Atlassian repo.
      It arrives as a provided-scope transitive of jira-api (supplied by the
      Jira runtime; this plugin never compiles against it), but Maven still
      must resolve it to build the dependency graph. This repo entry is the
      standard fix for that resolution.
    -->
    <repository>
      <id>jenkins-public</id>
      <url>https://repo.jenkins-ci.org/public/</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>atlassian-public</id>
      <url>https://packages.atlassian.com/mvn/maven-external/</url>
      <releases><enabled>true</enabled></releases>
      <snapshots><enabled>false</enabled></snapshots>
    </pluginRepository>
  </pluginRepositories>
</project>
